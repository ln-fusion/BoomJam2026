# BoomJam2026 Agent 工作契约

本文件适用于仓库根目录及其全部子目录。任何在本项目中工作的 Agent 都必须遵守本契约；子目录中的 `AGENTS.md` 只能补充或收紧要求，不能降低本文件的约束。

## 项目边界

- 仓库根目录是唯一 Unity 项目，必须包含 `Assets/`、`Packages/` 和 `ProjectSettings/`。
- 使用 Unity `2022.3.62f3`。不要创建、打开或修改嵌套的 `Change and Run/` Unity 项目。
- 只维护仓库拥有的源文件。不要修改 `Library/`、`Temp/`、`obj/`、`Logs/`、`UserSettings/` 等生成目录。
- 开始工作前检查 `git status`。保留用户已有修改，不回退、不覆盖、不顺手格式化无关文件。
- 未经用户明确要求，不执行提交、变基、强制检出或其他会改变 Git 历史的操作。

## 事实来源

编写或修改说明前，按以下顺序核实信息：

1. 阅读相关实现、接口、调用点和测试，确认代码当前真实行为。
2. 查阅 `Documents/` 中对应的技术设计、存档架构和开发计划，确认设计意图、术语及阶段边界。
3. 检查相邻模块的既有命名和文档风格，保持跨模块一致。

实现行为与设计文档不一致时，不得用注释掩盖差异。文档说明必须准确描述当前行为，并明确标注尚未落地的计划内容；影响任务目标的差异应在结果中报告。

## C# XML 文档

### 必须覆盖的声明

仓库拥有的所有 C# 脚本，包括运行时代码、编辑器代码和测试代码，都必须满足以下要求：

- 每个类、结构、接口、记录、委托和枚举声明都必须有 XML 文档。
- 每个方法都必须有 XML 文档，不区分可见性；包括构造函数、析构函数、运算符、转换运算符、显式接口实现和局部函数。
- 属性访问器由属性自身的文档覆盖，无需分别为 `get` 和 `set` 添加文档。
- 面向其他模块使用的属性、字段、事件和枚举成员必须有 XML 文档。私有字段仅在用途不直观时添加说明。
- 只有在继承文档与当前成员语义完全一致时才可使用 `<inheritdoc/>`；不得用它回避核实行为。

### 标签完整性

- 每个声明必须包含非空的 `<summary>`，或包含语义准确的 `<inheritdoc/>`。
- 每个形参必须有且仅有一个同名 `<param>`；名称必须与签名完全一致。
- 每个泛型形参必须有且仅有一个同名 `<typeparam>`；名称必须与签名完全一致。
- 返回值含义、失败哨兵值或 `null` 语义不直观时，必须使用 `<returns>` 说明。
- 调用方需要据此编写正确控制流时，使用 `<exception>` 说明真实会抛出的异常。
- 使用 `<see cref="..."/>`、`<paramref name="..."/>` 和 `<typeparamref name="..."/>` 引用代码符号，不在正文中手写容易失效的符号名称。

### 内容与风格

- 默认使用简洁、客观的中文，新增文档使用中文句号 `。`，同一声明内保持标点一致。
- 说明职责、输入、输出、副作用、失败方式、取消语义和生命周期；不要只是复述成员名称或类型。
- Unity 生命周期方法应说明触发时机和关键副作用；异步方法应说明取消令牌的作用；测试方法应说明场景和断言目标。
- 不得承诺实现中不存在的校验、线程安全、异常、持久化或业务流程。
- 对占位实现或阶段性行为，明确写出当前限制和对应阶段，不把未来设计描述成现状。
- 修改签名或行为时，必须在同一变更中同步更新 XML 文档和相关调用方文档。
- 纯文档任务不得改变运行逻辑。若确实发现必须修复的逻辑问题，应先报告并将逻辑修改与文档修改清晰分离。

## IDE 折叠约定

- Rider 中的文档注释必须默认折叠：`Editor | General | Code Folding | Documentation comments`。
- 对应 Rider 设置键为 `CodeFoldingSettings/COLLAPSE_DOC_COMMENTS=true`。
- 该选项属于本机 Rider 应用设置，无法可靠地通过解决方案 `.DotSettings` 共享。本契约是项目级要求；使用新环境时应检查本机设置。
- 不提交 `.idea/**/workspace.xml`、`.ai/` 或其他个人 IDE 状态文件。

## 验证要求

完成 C# 变更前必须执行与风险相匹配的验证：

1. 对所有仓库拥有的 `*.cs` 文件执行语法树级审计，不得只依赖正则表达式。
2. 审计至少覆盖类型、普通方法、构造函数、析构函数、运算符、显式接口方法和局部函数。
3. 验证 XML 可解析，并检查 `<param>`、`<typeparam>` 的名称、缺失和重复情况。
4. 让 Unity 完成脚本编译，确认 Console 无编译错误；修改运行时代码或测试代码后运行 EditMode 测试。
5. 执行 `git diff --check`，并检查最终差异中没有无关逻辑修改、生成文件或个人 IDE 文件。

若当前环境无法执行某项验证，必须在最终结果中明确说明未执行项、原因和剩余风险。完成报告应给出扫描文件数、审计声明数、问题数以及测试通过/失败数量。
